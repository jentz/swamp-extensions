{
  "extension": "@jentz/aws-rds-inventory",
  "version": "2026.07.20.1",
  "reviewedAt": "2026-07-20T09:44:06Z",
  "dimensions": [
    {
      "id": "credentials-secrets",
      "verdict": "pass",
      "note": "No credential/secret path touched. scanErrorKey re-encodes four already-operator-facing diagnostic segments (profile label, region, service, phase); none is token material, and AWS SDK credential resolution is unchanged."
    },
    {
      "id": "logging-quality",
      "verdict": "pass",
      "note": "No logging code changed. Log messages do not embed the storage key; the change only affects the stored scan_error row name, not any logged value."
    },
    {
      "id": "error-handling",
      "verdict": "pass",
      "note": "classifyError/collectHaystack and the throw-before-write ordering are unchanged. Only scanErrorKey's per-segment encoding changed; failures still throw before writeResource."
    },
    {
      "id": "testing-completeness",
      "verdict": "pass",
      "note": "_lib/scan_error_test.ts gains injectivity and determinism tests with teeth for the canonical key. Tests here are unaffected because this extension does not call the canonical scanErrorKey. All tests green."
    },
    {
      "id": "idempotency-resilience",
      "verdict": "pass",
      "note": "This extension does not use the canonical scanErrorKey, so re-run and overwrite behavior is unchanged; the regenerated twin (classifyError) is deterministic."
    },
    {
      "id": "api-contracts",
      "verdict": "pass",
      "note": "No SDK calls or request shapes changed; the scan_error kind enum and stored row shape are untouched."
    },
    {
      "id": "resource-management",
      "verdict": "pass",
      "note": "No clients, handles, or files are affected by a pure key-encoding change."
    },
    {
      "id": "published-surface-hygiene",
      "verdict": "pass",
      "note": "Only source change is the regenerated shared _lib/scan_error.ts twin, consumed here via aws_credentials.ts (classifyError); the scanErrorKey change is inert for this extension. No identifiers added. Manifest and model.version aligned at 2026.07.20.1."
    },
    {
      "id": "schema-strictness",
      "verdict": "pass",
      "note": "ScanErrorSchema and all resource schemas remain untouched strict z.object; the change is confined to the scanErrorKey pure function."
    },
    {
      "id": "lifetime-gc",
      "verdict": "pass",
      "note": "No resource/file spec lifetime or garbageCollection touched by this change."
    },
    {
      "id": "crud-completeness",
      "verdict": "na",
      "note": "Read-only fan-out scan (Describe*/Get* only); no create/update/delete lifecycle to complete. Unaffected by a key-encoding change."
    },
    {
      "id": "preflight-checks",
      "verdict": "pass",
      "note": "The SSO pre-flight and profile-suffix refusal are unchanged in code; unaffected by the scanErrorKey change."
    },
    {
      "id": "instance-names",
      "verdict": "pass",
      "note": "This extension does not call the canonical scanErrorKey; it consumes the shared twin only via aws_credentials.ts (classifyError). The regenerated scanErrorKey change is functionally inert here, so instance and scan_error row names are unchanged."
    },
    {
      "id": "data-access",
      "verdict": "pass",
      "note": "readResource/writeResource paths unchanged; only the scan_error row NAME encoding changed, not the stored attributes."
    },
    {
      "id": "version-upgrades",
      "verdict": "pass",
      "note": "model.version bumped to 2026.07.20.1 with a matching no-op identity upgrade entry appended (description records the regenerated scanErrorKey twin, no globalArguments schema changes). The upgrades-tail toVersion equals model.version, verified by the passing metadata test."
    }
  ]
}
